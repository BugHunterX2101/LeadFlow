{
  "name": "leadflow",
  "version": "1.0.0",
  "private": true,
  "workspaces": [
    "backend",
    "frontend"
  ],
  "scripts": {
    "build": "npm run build --workspace frontend",
    "test": "docker compose up -d postgres && docker compose run --rm backend npm test",
    "seed": "npm run seed --workspace backend",
    "dev:backend": "npm run dev --workspace backend",
    "dev:frontend": "npm run dev --workspace frontend"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "description": ""
}
